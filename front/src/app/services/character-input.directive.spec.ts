import { CharacterInputDirective } from './character-input.directive';

describe('CharacterInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterInputDirective();
    expect(directive).toBeTruthy();
  });
});
